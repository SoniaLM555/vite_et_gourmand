{% extends 'base.html.twig' %}

{% block title %}Détails de l'allergène{% endblock %}

{% block body %}
    <h1>Allergène</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ allergene.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_allergene_index') }}">Retour à la liste</a>

    <a href="{{ path('app_allergene_edit', {'id': allergene.id}) }}">Modifier</a>

    {{ include('allergene/_delete_form.html.twig') }}
{% endblock %}
