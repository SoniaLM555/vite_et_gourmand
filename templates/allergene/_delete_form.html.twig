<form method="post" action="{{ path('app_allergene_delete', {'id': allergene.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ allergene.id) }}">
    <button class="btn">Delete</button>
</form>
