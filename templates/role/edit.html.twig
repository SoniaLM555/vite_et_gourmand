{% extends 'base.html.twig' %}

{% block title %}Modifier le rôle{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Modifier le rôle : {{ role.libelle }}</h1>
        <a href="{{ path('app_menu_admin_index') }}" class="btn btn-secondary">Retour à l'accueil</a>
    </div>

    {{ include('role/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_role_index') }}">Retour à la liste</a>

    {{ include('role/_delete_form.html.twig') }}
{% endblock %}
