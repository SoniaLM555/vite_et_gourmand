{% extends 'base.html.twig' %}

{% block title %}Modifier le thème{% endblock %}

{% block body %}
    <h1>Modifier le thème : {{ theme.nom }}</h1>

    {{ include('theme/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_theme_index') }}">Retour à la liste</a>

    {{ include('theme/_delete_form.html.twig') }}
{% endblock %}
