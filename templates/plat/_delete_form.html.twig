<form method="post" action="{{ path('app_plat_delete', {'id': plat.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce plat ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plat.id) }}">
    <button class="btn">Supprimer le plat</button>
</form>
